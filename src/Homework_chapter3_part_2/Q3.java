/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package Homework_chapter3_part_2;

import java.util.ArrayList;
import java.util.List;
import java.util.stream.Collectors;

/**
 *
 * @author khalil
 */
public class Q3 {
    public static void main(String[] args) {
   /*  List<Employee>listEmployees=new ArrayList(); --An employee type list was created
        listEmployees.stream() --Stream was used to fetch the list from an employee
                .filter(e-> e.getSalary() >=800 && e.getSalary() <1200).--A filter function was used to filter salaries greater than or equal to 800 and less than 1200
                map(e-> new Employee (e.getId(), e.getName(), e.getDepartment(), e.getSalary()*1.02))--A map function was used to return the list with all information for each employee and salary become salary*1.02
                .collect(Collectors. groupingBy(Employee::getDepartment, Collectors.counting()))--The collect function was used to create a grouping of the list, then create a map from the treemap type give it both key and value for each employee
                .forEach((d, c) -> System.out.println("Dept: "+ d + ", Count: " + c)); --give it both key and value for each employee and print key and value According to department name.
    }*/
   
    
    }
}
